import CreateAppointmentService from './CreateAppointmentService';

describe('CreateAppointment', () => {
  it('should be able to create a new appointment', () => {
    //
  });

  // it('should not be able to create two appointments to the same hour', () => {
  //   expect(1 + 2).toBe(3);
  // });
});
